pipeline {
agent any
stages {
stage('Provision infrastructure') {
 steps {
   sh 'terraform init ./Actimize-master/Customers/HSBC'
   sh 'terraform plan -out=plan  ./Actimize-master/Customers/HSBC'

 }
}
  stage('Approval') {
      steps {
        script {
          def userInput = input(id: 'confirm', message: 'Apply Terraform?', parameters: [ [$class: 'BooleanParameterDefinition', defaultValue: false, description: 'Apply terraform', name: 'confirm'] ])
        }
      }
    }
stage('TF Apply') {
      steps {
        
          sh './Actimize-master/Customers/HSBC ; terraform apply plan'
        
      }
    }
}
}
 
